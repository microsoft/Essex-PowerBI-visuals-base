const package = require('./package');
const start = require('./start');
const installCertificate = require('./install-certificate');

module.exports = {
    installCertificate,
    package,
    start,
};
